{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'pdflatex.*resume'; then rm -f resume/resume.aux resume/resume.log resume/resume.out resume/resume.pdf 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
